export interface CheckoutFormData {
    firstName: string;
    lastName: string;
    emailAdress: string;
    address: string;
    city: string;
    postalCode: string;
    note: string;
    nameOnCard: string;
    cardNumber: string;
    expirationDate: string;
    cvc: string;
}

/**
 * nameOnCard
 * cardNumber
 * expirationDate
 * cvc
 * address
 * apartment
 * city
 * region
 * postalCode
 * sameAsShipping
 *
 */
