mutation CreateProductReview($review: ReviewCreateInput!) {
    review: createReview(data: $review) {
        ...reviewContent
    }
}

mutation CreateCheckout($checkout: CheckoutCreateInput!) {
    review: createReview(data: $checkout) {
        id
        stage
    }
}
